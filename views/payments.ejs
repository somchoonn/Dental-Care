<%- include('partials/head', { title: title }) %>
<section class="wrap">
  <div class="brand">
    <div class="logo">🦷</div>
    <div>
      <h1>ชำระเงิน</h1>
      <p>สรุปรายการค่ารักษาและยอดที่ต้องชำระของแต่ละคน</p>
    </div>
  </div>

  <div class="card">
    <% bills.forEach(b => { %>
      <div style="border:1px solid #eef0ff; border-radius:14px; padding:14px; margin-bottom:14px; background:#fcfcff;">
        <div class="row between" style="margin-bottom:6px;">
          <div><strong><%= b.patient.titleName %><%= b.patient.firstName %> <%= b.patient.lastName %></strong> · <span style="color:#667">ID: <%= b.patient.id %></span></div>
          <div><strong>รวม: <%= b.total.toLocaleString() %> บาท</strong></div>
        </div>

        <div style="overflow:auto;">
          <table style="width:100%; border-collapse:collapse;">
            <thead>
              <tr>
                <th style="text-align:left; padding:8px;">รายการ</th>
                <th style="text-align:right; padding:8px;">จำนวน</th>
                <th style="text-align:right; padding:8px;">ราคา/หน่วย</th>
                <th style="text-align:right; padding:8px;">เป็นเงิน</th>
              </tr>
            </thead>
            <tbody>
              <% b.items.forEach(it => { %>
                <tr>
                  <td style="padding:8px;"><%= it.name %></td>
                  <td style="padding:8px; text-align:right;"><%= it.qty %></td>
                  <td style="padding:8px; text-align:right;"><%= it.price.toLocaleString() %></td>
                  <td style="padding:8px; text-align:right;"><%= (it.qty*it.price).toLocaleString() %></td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>

        <div class="actions" style="margin-top:10px;">
          <a class="link" href="#">พิมพ์ใบเสร็จ</a>
          <button class="primary" type="button" onclick="alert('ชำระเงิน (เดโม) สำเร็จ')">ชำระเงิน</button>
        </div>
      </div>
    <% }) %>
  </div>
</section>
<%- include('partials/foot') %>
